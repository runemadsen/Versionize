<div class="grid_1">&nbsp;</div>
<div class="grid_5">
	
	<img src="/images/logo_beta.jpg" alt="Versionize" id="front_logo">
	
	<%= flash[:notice] %>
	<%= flash[:error] %>
	
	<p id="front_text">We are currently open for alpha-testers only. <%= link_to 'Visit the front page', '/' %> to subscribe yourself to the mailinglist. We will email you a password as soon as we have a beta ready.</p>
	
	<% form_for @user_session, :url => {:action => "create"} do |f| %>
	  <%#= f.error_messages %>
	
	  <%= f.text_field :email, :class => "front_input", :value => "Email", 	
						:onFocus => "if(this.value==this.defaultValue) this.value='';", 
						:onBlur => "if(this.value=='') this.value=this.defaultValue;" %><br />
	
	  <%= f.password_field :password, :class => "front_input input_password", :value => "Password",
						:onFocus => "if(this.value==this.defaultValue) this.value='';", 
						:onBlur => "if(this.value=='') this.value=this.defaultValue;" %><br />
	  
		<%= f.check_box :remember_me, :class => "input_remember" %><%= f.label :remember_me, :class => "input_remember_label" %>
		<%= f.submit "Login", :class => "input_submit" %>
	<% end %>

	<%#= link_to "Don't have an account? Create a user", :controller => :users, :action => :new %>
	
</div>
	
<div class="grid_6" id="front_boxes">
	
</div>
<div class="clear"></div>

