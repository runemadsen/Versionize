<div id="description" class="idea_item">
	<%= h(text.body).gsub(/\n/, '<br/>') %>

	<% if edit %>
		<div class="edit_item">
			<%= link_to "Edit", edit_idea_text_path(@idea, text.id) %>
		</div>
	<% end %>
</div>