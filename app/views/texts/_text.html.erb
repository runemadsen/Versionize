<div class="idea_item">
	<%= text.body %>
	
	<% if edit %>
		<br /><br />
		<%= link_to "Edit", edit_idea_text_path(@idea, text.id) %>
		<%= link_to "Destroy", idea_text_path(@idea, text.id), :method => :delete %>
	<% end %>
</div>