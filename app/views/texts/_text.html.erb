<div class="idea_item">
	<%= text.body %>
	
	<% if edit %>
		<div class="edit_item">
			<%= link_to "Edit", edit_idea_text_path(@idea, text.id) %>
			<%= link_to "Delete", idea_text_path(@idea, text.id), :method => :delete, :class => "destroy" %>
		</div>
	<% end %>
</div>