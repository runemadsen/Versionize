<div class="grid_12">
	
<h1>Edit Text</h1>

<%= form_tag idea_version_text_path(@idea, @version.alias, @text), :method => :put do %>

	<%= text_area_tag :body, @text.body, {:class => "input_box input_text"} %>
	<br /><br />
	<%= submit_tag 'Save text' %><br /><br />
	
<% end %>

<%= button_to "Delete Text", idea_version_text_path(@idea, @version.alias, @text), :confirm => "Are you sure you want to delete this text?", :method => :delete %>

</div>
<div class="clear"></div>